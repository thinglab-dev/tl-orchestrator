## Deferred from: code review of 6-0a-configuracao-e-lifecycle-do-control-plane (2026-09-08)

- ~~DW-6.0A-01~~ — **RESOLVIDO em 2026-09-08.** Correção própria do Orquestrador: o literal exato `"uso: lynvia-mkauth-connector [--version]"` foi trocado pelo prefixo estável `"uso: lynvia-mkauth-connector [--version"` (sem o `]` de fechamento), que continua substring de qualquer usage futura que preserve esse prefixo, sem fixar o conjunto de subcomandos. Prova: reproduzido vermelho antes; verde isolado depois; sonda contrafactual mutando temporariamente a usage real do CLI do outro módulo confirmou que a asserção nova ainda discrimina regressão real (voltou a ficar vermelho), restaurado por hash; `make check` completo do módulo `lynvia`: exit 0, `LYNVIA_GATE_COMPLETE`, 0 SKIP. Revisão externa: [parecer](pareceres/checker-dw-6-0a-01-2026-09-08.md) — Checker Agy `gemini-3.8-flash-medium`, `approved`, zero achados. Diff: 1 arquivo, `internal/connectorgate/release_test.go`, 1 linha.

- DW-6.0A-01 — `internal/connectorgate/release_test.go:243` (`TestEntrypointDoAgenteExecutaComoSubprocesso`): o oráculo espera `uso: lynvia-mkauth-connector [--version]`, mas o CLI real de `modules/lynvia-mkauth-connector/cmd/lynvia-mkauth-connector/main.go:133` imprime corretamente `uso: lynvia-mkauth-connector [--version|install|uninstall|mariadb-bootstrap|recover|update-selfcheck|update-reconcile|admin]` desde que aquele módulo ganhou esses subcomandos em stories próprias (1.6-1.15d). O teste compila e executa o binário do outro módulo (`buildHostEntrypoint`, `./cmd/lynvia-mkauth-connector`) e ficou com o oráculo desatualizado; não é regressão de código do binário nem de nenhuma story deste módulo. Confirmado presente na árvore limpa antes de qualquer alteração de 6.0a e inalterado por ela. **Bloqueia `make check` do módulo `lynvia` inteiro** (o alvo `test` do Makefile reprova em qualquer `--- FAIL`, sem distinguir pacote). **Condição de quitação:** atualizar o literal esperado em `release_test.go` para a usage atual do CLI do conector (ou tornar o oráculo derivado da flag real, não hardcoded), com prova de que o teste ainda discrimina uma regressão de usage. Dono: quem tocar `internal/connectorgate` a seguir, ou story dedicada de manutenção do gate.
