# Evidência de Conclusão — Task T027

- **Task:** T027 (Mandatory Write-Ahead Journaling and Budgeted Dispatch for Automatic Mode)
- **Data:** 2026-09-14
- **Autores:** google
- **Status:** done
- **Checker:** auto-verificação determinística via 291 testes unitários e `validate_repository.py`

## 1. Escopo Entregue

1. **Primitive Unificado `budgeted_model_dispatch` (`scripts/tl_job.py`):**
   - Implementado primitive de biblioteca padrão garantindo o ciclo invariante:
     `reserve -> pending_call persistido -> dispatch -> resultado observado -> consumed += 1 -> reserved -= 1 -> pending_call = null -> persistência em disco`.
   - Subcomando CLI `tl_job.py budgeted-dispatch` adicionado para invocação direta por scripts/condutores.
   - Suporte universal aos 6 papéis: `classifier`, `planner`, `maker`, `checker`, `advisor` e `searcher`.
   - Suporte nativo à contabilidade individual de cada tentativa real (incluindo retries decorrentes de erro de validação de schema).
   - Recálculo de `required_call_reserve` antes de autorizar retries, com STOP imediato por `insufficient_budget_for_unit_verification`.
   - Tratamento de falhas pré-despacho (`PRE_DISPATCH_UNAVAILABLE`) com liberação de reserva e sem consumo de chamada.
   - Tratamento de desfecho ambíguo com débito conservador de chamada e STOP por `unrecoverable_harness_failure`.

2. **Guarda T016 nos Briefings e Contratos do Maker:**
   - `prompts/maker.md` atualizado com instrução explícita para testes direcionados (*targeted verification*) em etapas intermediárias intragrupo.
   - Vedada a instrução para o Maker executar `make check` / `make dev-check` completos em ambientes sandbox sem permissão de sockets/rede.
   - `prompts/orchestrator-playbook.md` atualizado para exigir que os briefings forneçam comandos de testes direcionados.

3. **Governança e Documentação:**
   - Atualizados `docs/WORK_MODEL.md`, `docs/EXECUTION_PROTOCOL.md`, `prompts/orchestrator.md` e `CHANGELOG.md`.
   - Registrada Task T028 em `_tl-orc/project/tasks/` para binding do commit aprovado pelo Checker e delta pós-revisão.

## 2. Verificação Mecânica

- `python3 scripts/validate_repository.py`: OK (24 arquivos de pacote conferidos).
- `python3 -m unittest discover -s scripts/tests`: 291 testes PASS (0 falhas).
